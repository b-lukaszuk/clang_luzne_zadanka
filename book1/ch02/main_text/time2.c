#include <stdio.h>
#include <stdlib.h>

int main(void) {

  int hour = 11;
  int minute = 59;
  char colon = ':';

  printf("The hardcoded time is %i%c%i\n", hour, colon, minute);

  return (EXIT_SUCCESS);
}
